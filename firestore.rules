rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to login attempts collection
    match /loginAttempts/{email} {
      allow read, write: if true;
    }
    
    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
